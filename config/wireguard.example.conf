# Replace every PLACEHOLDER_* value before starting the account.
# Keep this file as an example only. Real configs live in state/accounts/<name>/wireguard.conf.

[Interface]
PrivateKey = PLACEHOLDER_PRIVATE_KEY
Address = 10.0.0.2/32

[Peer]
PublicKey = PLACEHOLDER_SERVER_PUBLIC_KEY
AllowedIPs = 0.0.0.0/0, ::/0
Endpoint = 198.51.100.1:51820
PersistentKeepalive = 25
